# Optional / heavier packages. Installed best-effort by the setup script.
# A failure here (e.g. no wheel yet for a brand-new Python) does NOT block the
# collector — the pipeline falls back gracefully.

# NLP feature extraction (extract_features.py): POS, dependency parse, lemmas,
# morphology. Also used for sentence segmentation in compute_metrics.py.
# REQUIRES a model — install once after spaCy:
#     python -m spacy download en_core_web_sm
spacy>=3.7

# Readability (compute_metrics) + VADER rule-based sentiment (extract_features).
# VADER needs a one-time lexicon download (auto-fetched on first use):
#     python -c "import nltk; nltk.download('vader_lexicon')"
textstat>=0.7
nltk>=3.8

# Embeddings for the later analysis phase (semantic drift, etc.). Pulls PyTorch.
sentence-transformers>=2.7

# Local LLM client — points at your LM Studio OpenAI-compatible endpoint.
openai>=1.30

# Statistics + figures + modeling for the analyses (replicate_berisha.py,
# validation_figure.py, cohort_figures.py, compare_features.py, latent_factors.py,
# latent_trajectory.py) and the exploration notebook.
scipy>=1.11
matplotlib>=3.7
scikit-learn>=1.3
statsmodels>=0.14    # LOWESS smoothing (latent_trajectory.py)
jupyterlab>=4.0      # notebooks/explore_corpus.ipynb

# Postgres store + web GUIs that query it DIRECTLY (no mirror).
#   load_to_postgres.py  -> Postgres system of record
#   dashboard.py         -> marimo reactive dashboard (serve_gui.sh)
#   pgweb                -> install separately: `brew install pgweb`
psycopg[binary]>=3.1
sqlalchemy>=2.0
marimo>=0.23
altair>=5.0

# Optional legacy: Datasette browser over a SQLite mirror (build_browser.sh).
datasette>=0.64
sqlite-utils>=3.36
db-to-sqlite[postgresql]>=1.5

# --- handy for analysis notebooks (uncomment as desired) ---
# matplotlib>=3.8
# scikit-learn>=1.4
# plotly>=5.20
# duckdb>=0.10
# polars>=0.20
# networkx>=3.2
